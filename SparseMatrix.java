import java.io.File;

public class SparseMatrix{
    //store in compressed sparse column format

    public SparseMatrix(File file){
        //read the file and store the matrix in compressed sparse column format
    }
}